<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: blueviolet;
        }
    </style>
</head>

<body>
    <h1>URL Shortener</h1>

    <h2>All URLs:</h2>
    <ul>
        <% urls.forEach(url=> { %>
            <li>
                <a href="<%= url.redirectURL %>">
                    <%= url.shortId %>
                </a>
            </li>
            <% }) %>
    </ul>

    <form action="/generate-url" method="POST">
        <label for="url-input">Enter your Original URL:</label>
        <input type="text" id="url-input" required name="originalURL" placeholder="https://example.com">
        <button type="submit">Generate</button>
    </form>
</body>

</html>